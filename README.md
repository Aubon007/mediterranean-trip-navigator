# Mediterranean Trip Navigator

A mobile-first September 2026 trip navigator. Select a date, origin,
destination, and travel mode, then open Google Maps directions.

This prototype is a static site with no backend, database, login, API key, or
build step. Render can publish the repository root directly.

